# 「心动概率局」产品全面升级执行提示词

## 新窗口下达指令

在新的 Codex 窗口中，将工作目录设置为 `F:\AI\择偶概率软件`，然后只需发送下面这句话：

> 请先完整读取并严格执行 `F:\AI\择偶概率软件\产品全面升级执行提示词.md` 中的全部要求，将其视为本次任务的主执行规范。现在立即开始，不要只给方案，不要阶段性等待我回复；持续完成核查、研究、修复、重构、优化、测试和最终交付。全程授权你在 `F:\AI\择偶概率软件` 内工作。

---

## 1. 任务身份与最终目标

你现在是这个产品的总负责人，同时承担产品经理、统计建模负责人、数据研究员、UX 设计师、视觉设计师、前端架构师、测试工程师和发布工程师的职责。

工作目录：

`F:\AI\择偶概率软件`

核心任务：

对现有「心动概率局」进行一次端到端、全方面、真正落地的成熟化升级。不要只输出审计报告或优化方案；完成核查后必须直接修复、重构、优化、测试，交付一个可运行、可维护、可解释、可验证、接近正式发布标准的成熟产品。

本任务是一个需要持续推进的完整目标。不要把“写出方案”“修完几个问题”“构建通过”当作完成。只有产品、模型、数据、维度、交互、视觉、隐私、工程、测试和文档形成完整闭环，才算完成。

遵守工作区内的 `AGENTS.md` 及上级指令。不得使用或恢复 Superpowers 工作流及其捆绑的规划、TDD、调试、评审或验证技能；其他独立且适用的 Codex 技能可以正常使用。

## 2. 授权范围与自主工作方式

用户已授权你在 `F:\AI\择偶概率软件` 内自主完成以下工作：

- 阅读、创建、修改、移动和整理项目文件。
- 全面审查现有产品、代码、算法、数据、交互和视觉。
- 联网查找并核验尽可能新的公开数据、官方报告和学术资料。
- 安装、升级、替换或删除项目依赖。
- 重构前端架构、计算引擎、数据结构、状态管理和页面结构。
- 增加测试、脚本、文档、配置和本地质量门禁。
- 运行开发服务器、构建、测试、浏览器自动化、无障碍检查和视觉检查。
- 在项目没有版本控制时建立本地 Git 基线和安全回退点。
- 使用并行子任务加速产品研究、数据核验、算法复核、UX 审查和测试。

不要在审计完成、方案完成、P0 完成、模型完成或界面完成等阶段停下来要求用户批准。先核查，形成内部方案，然后自动连续实施，直至最终验收。

工作过程中可以发送简短进度更新，但这些更新不能变成等待用户回复的关卡。遇到非关键歧义时，依据以下优先级自行作出可逆、合理的决定并记录理由：

1. 本提示词明确的产品目标与原则。
2. 当前产品已经表现良好的特色和用户心智。
3. 数据证据、模型正确性与真实用户体验。
4. 隐私最小化、可维护性、可测试性和可回退性。
5. 成熟产品的通行做法。

只有出现以下真实阻断时才允许向用户提问：

- 必须使用用户未提供的外部账号、密钥、付费服务或身份认证。
- 必须执行工作目录以外的不可逆操作。
- 必须正式发布到公网、购买服务、上传文件、发送数据或修改外部账户。
- 已穷尽安全替代方案，仍无法继续关键工作。

即使遇到上述阻断，也应先完成其他不受阻的工作，不要提前停止整个任务。

本次授权仅限工作目录内部。不得擅自上线、购买服务、向第三方上传用户数据、修改外部账户或做其他超出授权范围的操作。

保护现有文件和敏感信息：

- 不读取后在输出中展示 `.env`、令牌、密钥和账号凭据的值。
- 建立 Git 基线前先检查和完善 `.gitignore`。
- 不提交 `node_modules`、构建产物、缓存、日志和敏感配置。
- 不做无依据的大规模删除。
- 清理未使用文件和依赖前，必须确认没有真实引用。
- 保留可恢复的基线和清晰的变更记录。

## 3. 产品定位与不可偏离的原则

产品定位为：

> 匿名、本地计算、以真实统计数据和透明模型为基础，同时具备网感、梗感和传播力的轻娱乐择偶条件分析产品。

它不是：

- 科研级人口预测系统。
- 婚恋撮合平台。
- 对具体个人感情结果的预测工具。
- 给人划分价值高低的评分系统。
- 必须依赖账号、数据库、云存储和运营后台的 SaaS 产品。

产品应让用户感到：

- 好玩、有梗、愿意继续操作。
- 结果有一定现实参考价值。
- 能看懂数据从哪里来、模型做了什么。
- 能区分直接数据、模型推算和娱乐假设。
- 不会被虚假的精确数字欺骗。
- 不会因为某个身份、身体状况或经济条件被羞辱。
- 愿意查看结果、调整条件和生成战报分享。

核心表达原则：

- 可以幽默，但不能拿人的尊严、疾病、性取向、婚史、收入、身体状况或亲密能力进行恶意羞辱。
- 梗可以针对“条件组合太逆天”“模型开始冒烟”“用户疯狂叠 Buff”，不要针对某类人的人格和价值。
- 始终传达“稀有的是条件组合，不是人的价值”。
- 保持轻松、有网感、有传播力，不要升级成冰冷的企业数据仪表盘。
- 数据尽可能贴近现实，但绝不能把估算伪装成确定事实。

尽量保留并升级以下现有特色：

- 马卡龙手绘贴纸风。
- 小人漏斗和条件变化动画。
- 实时结果反馈。
- 稀有度等级。
- 有梗的结果文案。
- 反向自评。
- 分享战报。
- 数据来源说明。
- 移动端底部结果入口。
- 桌面端常驻结果面板。

## 4. 执行顺序：内部阶段连续推进，不等待用户回复

按照以下顺序持续推进，阶段之间不得停下来等待批准：

1. 建立安全基线，运行现有项目，记录升级前行为与指标。
2. 全面审计产品、模型、数据、代码、交互、视觉、隐私和工程。
3. 复现“稍微选几个条件就没人”的问题并定位真实根因。
4. 重建维度分类、数据证据体系和概率模型。
5. 大幅扩充并细化维度库。
6. 重构产品信息架构、交互流程和状态体系。
7. 升级视觉系统、品牌表达、响应式和无障碍体验。
8. 升级隐私、分享、错误恢复和安全设计。
9. 完成工程架构、依赖、测试、质量门禁和文档成熟化。
10. 运行完整验证，修复失败项，生成前后对比和最终交付说明。

审计与设计产物要用于后续实施，而不是写完后束之高阁。

## 5. 最高优先级：彻底核查“稍选几个条件就没人”的问题

目前最需要解决的问题是：用户稍微选择几个条件，结果就迅速接近 0 人或没有符合者。

不要先假设一定是算法错误，也不能为了结果好看而简单调大概率。必须判断它究竟属于：

- 现实中确实非常稀少。
- 基础人口池计算错误。
- 数据年份、地域、样本和统计口径不一致。
- 相关条件被重复相乘。
- 把所有条件错误地当作统计独立。
- AND、OR、嵌套、包含和互斥逻辑错误。
- 条件对应的人群分母不同。
- 城市、年龄、性别和婚史缩放错误。
- 收入、资产、住房、学历、职业等高度相关因素重复扣减。
- 健康、年龄、生活方式、发量、睡眠等相关因素重复扣减。
- 软偏好被错误地当作硬筛选。
- 娱乐维度进入了严肃人口估算。
- 极小概率的显示、舍入或下限处理错误。
- 数据没有覆盖某个输入，却被错误地当作概率 0。
- 所谓误差区间没有真实校准依据。
- 当前测试存在盲区，给出了虚假的安全感。

先建立升级前的代表性场景集，至少包括：

- 完全不增加附加条件的基础人群。
- 只增加一个常见条件。
- 逐步增加 3、5、8 个常见条件。
- 收入、资产、住房、学历同时存在的高相关组合。
- 年龄、健康、运动、睡眠同时存在的高相关组合。
- 单城市、多城市和全国场景。
- 男性、女性和不同年龄段场景。
- 现有快捷预设场景。
- 边界年龄和极端门槛场景。
- 软偏好很多但硬条件很少的场景。

对每个场景逐步记录：

- 初始基础池。
- 每个条件加入前后的人数。
- 使用的概率或条件概率。
- 数据来源和适用口径。
- 所属相关性分组。
- 为什么能够或不能与其他条件相乘。
- 对最终结果的边际影响。
- 乐观、基准和保守估算。
- 最终结果是否超过模型的可靠分辨率。

必须重新验证并修复以下已知风险：

1. UI 年龄允许选择 18–50 岁，但人口模型可能只覆盖到 49 岁，导致 50–50 岁基础池为 0。
2. 18–50 岁每个单岁人口池必须有效，年龄区间合并应基本守恒。
3. 婚史全部取消后，界面状态和模型是否偷偷回退为“未婚”。
4. 星座等枚举测试是否只验证数组长度，没有验证合法值。
5. 学历、院校、收入、资产、房产、车辆和职业是否重复扣减。
6. 年龄、健康、脱发、睡眠、运动和亲密关系是否重复扣减。
7. 全国、城市、多城市组合，以及城市工资和人口缩放是否合理。
8. 某些维度对特定性别或年龄不适用时，是否仍错误进入计算。
9. 现有预设为什么会快速得到十几人、零点几人或接近 0 人。
10. 当前所谓“±2.5 倍误差区间”是否存在可以复现的校准依据。
11. “互相心动概率”是否只是两个人群比例相乘，从而造成概念性误导。
12. 极端门槛下显示“0 人”，是否只是期望值低于 1 或数据精度不足。

常见组合不得在没有合理证据的情况下出现明显荒谬结果；极端组合也不能为了迎合用户被人为调高。所有修正必须能被数据、模型假设或明确的产品语义解释。

## 6. 重构模型：禁止所有维度无脑连乘

将全部维度明确划分为至少四类：

1. **硬筛选条件**  
   可以合理用于估算满足条件的人口数量，例如年龄、地区、婚姻状态和明确身高范围。

2. **相关硬条件**  
   会影响人口数量，但必须通过条件概率、联合分布、分层模型或相关性修正计算，例如学历、职业、收入、资产、住房、车辆、年龄和健康。

3. **软偏好**  
   不应直接把人口池砍掉，而应进入综合匹配度，例如沟通方式、消费观、家庭边界、情绪表达、兴趣和生活节奏。

4. **娱乐条件**  
   单独生成趣味指数或彩蛋，不进入可信人口估算，例如星座、MBTI 娱乐玩法和其他网络梗维度。

最终结果至少拆成：

- 满足硬条件的估算人群。
- 综合匹配度或软条件契合度。
- 娱乐指数。
- 双向条件命中示意。

不要继续把软偏好包装成确定人口概率。

彻底复核“互相心动概率”的统计含义。如果无法证明它能代表真实心动概率，应改为更准确但仍有传播力的名称，例如：

- 双向条件命中率。
- 双向上头指数。
- 双方偏好交集估算。
- 缘分交集指数。

同时显著说明它不是对具体感情结果的预测。

根据问题本身选择可解释、可测试、可校准、可维护的方法，可以考虑但不限于：

- 条件概率。
- 分层人口模型。
- 分段插值。
- 地区、年龄和代际修正。
- 贝叶斯网络。
- Copula 联合分布。
- 相关性分组。
- 蒙特卡洛模拟。
- 灵敏度分析。
- 宏观总量校准。

不要为了显得高级而堆复杂数学。每种方法都必须能解释“为什么适合这里”。

如果采用随机模拟：

- 支持固定随机种子。
- 同一输入得到稳定结果。
- 测试中结果可复现。
- 说明样本量、误差和收敛方式。

对于小于 1 人的结果，不要直接显示“现实中不存在”，应显示类似：

> 模型期望值低于 1 人，已经超出当前数据的可靠分辨能力。这不代表现实中绝对不存在。

同时展示：

- 乐观、基准、保守范围。
- 哪些条件造成最大下降。
- 去掉或放宽某个条件后的变化。
- 当前模型可信度。
- 数据版本和计算版本。

## 7. 数据研究与“合理设想”的规则

现实数据经常只有全国平均、粗粒度年龄段或单一报告。不能因为报告死板就直接套用，也不能因为缺少数据就随意编数字。

联网查找并核验尽可能新的公开资料。来源优先级为：

1. 国家统计部门和政府主管部门。
2. 人口普查、统计年鉴和行政数据。
3. 权威学术论文、系统综述和大样本调查。
4. 权威研究机构和行业报告。
5. 多来源侧面印证。
6. 证据不足时建立透明、可替换的模型假设。

搜索技术和统计问题时优先使用原始、权威来源，不依赖二手营销文章。记录来源的真实 URL 和检索日期，遵守引用与版权边界，不复制受版权保护的长篇原文。

对每一个进入人口计算的维度，建立机器可读的数据证据登记表，至少包含：

- 唯一 ID。
- 维度名称和精确定义。
- 适用人口。
- 地域。
- 年龄范围。
- 性别统计口径。
- 数据年份。
- 发布机构。
- 原始来源 URL。
- 检索日期。
- 样本和分母。
- 直接统计值。
- 从原始数据到模型参数的转换公式。
- 是否使用插值、外推或相关性修正。
- 基准值。
- 乐观值。
- 保守值。
- 可信等级。
- 数据局限。
- 模型版本。
- 数据版本。

数据等级至少区分：

- A：官方或权威资料直接给出的相同口径数据。
- B：权威数据经过简单、透明、可解释的转换。
- C：多个相关来源交叉推算。
- D：娱乐性假设或弱证据估计。

页面上使用简洁徽标表达等级，不要把所有方法细节堆在主界面；用户应能继续展开查看完整方法和来源。

当缺少直接数据时允许进行“合理设想”，但必须：

- 使用多个间接锚点交叉验证。
- 给出范围而不是单个假精确值。
- 明确关键假设。
- 做年龄、地区、性别或代际修正。
- 检查与宏观人口总量是否自洽。
- 支持未来替换和版本升级。
- 在页面上明确标为模型推算。
- 不用“官方数据”统称所有结果。

如果某个维度完全缺乏可信人口比例，不要硬编成硬筛选概率，应将其改为软偏好或娱乐维度。

建立明确的 `modelVersion` 和 `dataVersion`。数据或模型变化时生成影响报告，列出典型场景升级前后的结果差异。

## 8. 大幅扩充和细化选择维度

现有维度不够丰富，需要显著扩充，但不能简单堆一百个按钮。目标是建立一个可搜索、可分类、可解释、可持续扩展的原子维度库。

至少覆盖以下类别，并根据数据可用性和真实产品价值继续补充：

### 8.1 基础人口与地域

- 年龄、居住城市、户籍或成长地区。
- 城市级别、是否接受异地。
- 婚姻状态、是否有子女。
- 可接受年龄差。
- 定居意愿。

### 8.2 外形与身体特征

- 身高范围，而非只有最低值。
- 体型、BMI、训练习惯。
- 发量、发际线、牙齿、视力。
- 穿搭、整洁度、形象管理。
- 对外形要求的强弱等级。

### 8.3 教育与学习

- 学历。
- 院校层级。
- 专业大类。
- 学习能力和终身学习习惯。
- 学历是否为硬条件。

### 8.4 工作与职业

- 职业大类和行业。
- 体制内、国企、民企、外企、自由职业和创业。
- 工作稳定性。
- 工作强度。
- 加班、出差、夜班、异地和远程。
- 管理岗位、专业岗位。
- 收入发展趋势。

### 8.5 收入与财务

- 个人税前年收入。
- 税后可支配收入。
- 收入稳定性。
- 家庭资产和个人资产。
- 负债、房贷和消费贷。
- 储蓄率。
- 消费观。
- 是否愿意共同理财。
- 财务透明度。

### 8.6 房产与交通

- 是否有房。
- 房产归属、贷款情况。
- 城市、地段、面积和类型。
- 是否接受婚后共同购房。
- 是否有车、车辆用途。
- 通勤方式和时长。

### 8.7 生活方式

- 吸烟、饮酒。
- 睡眠作息。
- 运动频率。
- 饮食偏好。
- 做饭、家务和整洁。
- 宠物。
- 游戏、追剧、旅行和夜生活。
- 社交频率。
- 手机和网络使用习惯。

### 8.8 家庭背景与边界

- 是否独生子女。
- 兄弟姐妹情况。
- 父母养老和退休保障。
- 原生家庭经济负担。
- 家庭关系密度。
- 婚后是否同住。
- 对父母介入关系的边界。
- 赡养安排。

### 8.9 婚恋目标与未来规划

- 恋爱、结婚或开放了解。
- 结婚时间预期。
- 是否要孩子、计划几个。
- 育儿方式。
- 定居城市。
- 买房计划。
- 职业与家庭优先级。
- 家务和照护分工。

### 8.10 性格与沟通

- 大五人格或其他可解释的人格轴。
- 外向程度。
- 情绪稳定性。
- 冲突处理方式。
- 沟通频率。
- 表达需求的方式。
- 道歉、修复和冷静时间。
- 依恋与边界。
- 是否接受长时间独处。

### 8.11 价值观与关系观

- 忠诚观。
- 金钱观。
- 消费与储蓄观。
- 性别角色观。
- 家庭责任。
- 个人成长。
- 对伴侣事业的支持。
- 隐私和社交边界。
- 重大决定方式。

### 8.12 健康与敏感信息

- 慢性健康状况。
- 运动能力。
- 睡眠。
- 视力、牙齿等身体状况。
- 心理健康与压力管理。
- 生育和亲密关系相关偏好。
- 性取向和关系边界。

### 8.13 兴趣与社交

- 旅行、运动、音乐、阅读、游戏和摄影等兴趣。
- 社交圈大小。
- 聚会频率。
- 是否接受不同兴趣。
- 共同活动需求。

### 8.14 娱乐维度

- MBTI。
- 星座。
- 血型。
- 网络人格梗。
- 其他趣味测试。

维度数量应显著增加，形成丰富的原子维度库，但每一项必须定义：

- 唯一 ID。
- 分类。
- 适用范围。
- 硬条件、相关硬条件、软偏好或娱乐条件标签。
- 单选、多选、范围、等级或其他输入类型。
- AND、OR、互斥、包含或嵌套语义。
- 数据来源或假设等级。
- 隐私敏感等级。
- 是否进入人口估算。
- 是否进入匹配评分。
- 是否允许进入分享战报。

不要为了数量强行增加重复、冒犯、无法解释或无法建模的选项。

敏感维度需要保留并扩充，但应默认折叠、显著标注、由用户主动展开。产品面向成年人，加入适当且不打扰体验的成年用户提示。

## 9. 重构信息架构与核心交互

采用“移动端优先的分步式单页应用”。不要继续把全部内容平铺成八九千像素的超长页面，也不要做成频繁跳转的传统多页问卷。

推荐流程：

1. 欢迎和产品边界说明。
2. 基础人口范围。
3. 核心硬条件。
4. 进阶维度库。
5. 敏感与娱乐条件。
6. 结果解释。
7. 方案调整、对比和分享。

交互要求：

- 在同一应用中切换步骤。
- 用户可以自由前进、返回和修改。
- 桌面端结果面板持续可见。
- 手机端保留底部结果胶囊和可展开结果面板。
- 结果从任意位置一键可达。
- 显示当前步骤和整体进度。
- 增加已选条件汇总栏。
- 支持搜索维度和按类别过滤。
- 支持单项删除。
- 支持撤销和重做。
- 支持一键清空，并做合理确认。
- 支持条件强弱等级。
- 在模型允许的情况下，支持“设为硬条件或偏好条件”。
- 支持快捷预设，预设覆盖需要安全确认。
- 支持只看最影响结果的条件。
- 支持一键放宽某项条件。
- 支持展示放宽前后差异。
- 在有真实用户价值时支持方案 A/B 对比。
- 输入状态应能在当前会话中安全恢复。
- 不默认把敏感条件写入 URL、日志或云端。

解决现有交互问题：

- 默认已经存在基础范围，但底栏却显示“待开筛”的语义冲突。
- 无附加条件时点击移动端结果栏可能没有有效反馈。
- 婚史全部取消后的可见状态和实际计算不一致。
- 年龄上下限滑杆缺少明确标签。
- OR、AND、互斥和嵌套规则依赖很小的长文解释。
- 城市搜索没有无结果状态。
- 数字输入缺少就地校验。
- 分享只有 loading，没有成功、失败、重试和降级。
- 极端结果没有模型分辨率解释。
- 缺少完整的初始、空、错误、加载、成功和恢复状态。

结果页应清楚展示：

- 当前基础人口范围。
- 满足硬条件的估算人数。
- 乐观、基准和保守范围。
- 综合匹配度。
- 娱乐指数。
- 条件影响排行榜。
- 最大限制条件。
- 条件相关性说明。
- 数据可信等级。
- 模型版本和数据版本。
- 放宽建议。
- “为什么是这个结果”。
- 结果不代表人的价值，也不代表真实婚恋成功率。

## 10. 升级视觉系统、使用感受和品牌表达

保留马卡龙、手绘、贴纸和趣味动画特色，但提升为更成熟、统一、克制的视觉系统。

重点检查和解决：

- 所有卡片视觉权重相同。
- 卡片、倾斜、旋转、彩带、贴纸和小人同时抢夺注意力。
- 页面大量硬编码颜色、字号、边框、圆角和阴影。
- 辅助文字过小、透明度过低。
- Emoji、手绘图和组件图标混用。
- 动画过多时影响阅读和操作。
- 桌面与手机的信息层级不够清楚。

建立并实际使用语义设计系统，至少包括：

- 品牌色。
- 页面背景。
- 主次表面。
- 文字层级。
- 边框。
- 交互状态。
- 成功、提醒、风险和娱乐色。
- 字体体系。
- 字号和行高。
- 间距。
- 圆角。
- 阴影。
- 动效时长和缓动。
- 响应式断点。
- 层级和浮层规则。

业务组件不得继续大量写死十六进制颜色和魔法数值。

视觉优先级应为：

1. 当前估算结果。
2. 当前步骤和核心操作。
3. 已选条件。
4. 条件内容。
5. 数据解释。
6. 娱乐装饰。

动画必须服务于反馈：

- 选择条件时有即时反馈。
- 人群变化时有平滑过渡。
- 极端结果可以有克制的戏剧效果。
- 持续旋转、漂浮和彩带不得干扰操作。
- 完整支持 `prefers-reduced-motion`。
- 动画和随机装饰不得造成 React 渲染不稳定。

文案保持有网感、有梗，可以采用类似以下方向，但不要机械照抄：

- “条件正在叠 Buff”。
- “模型 CPU 快烧了”。
- “不是宇宙没货，是数据分辨率到头了”。
- “这一刀有点狠”。
- “放宽一格，地球重新有人了”。
- “条件组合稀有，不代表谁高谁低”。

避免：

- 把疾病、收入、性功能、婚史或性取向当作羞辱点。
- 暗示某类人低人一等。
- 用虚假确定性制造焦虑。
- 把人口统计比例直接称为爱情成功率。

## 11. 隐私、分享与信任设计

默认原则：

- 计算在本地完成。
- 不上传用户筛选。
- 不记录敏感字段。
- 不默认持久化敏感内容。
- 不引入账号、数据库或后端，除非存在无法替代的明确需要。
- 错误日志不得包含原始筛选和个人资料。

在页面合适位置清楚说明：

- 是否保存。
- 是否上传。
- 是否调用第三方服务。
- 数据在哪里计算。
- 如何清除本地状态。
- 分享图片会包含哪些内容。

分享功能必须升级：

- 生成前提供预览。
- 明确列出将公开的字段。
- 收入、资产、健康、亲密关系、性取向和个人自评默认不进入分享。
- 用户主动选择后才能加入敏感内容。
- 支持隐藏人数或部分条件。
- 提供成功提示。
- 提供失败提示和重试。
- Canvas 不可用时提供合理降级。
- 分享图带数据版本和“仅供娱乐参考”。
- 分享图不得误导为官方结论。

尽量将字体等资源自托管，减少第三方请求和国内网络不稳定。如果保留第三方资源，必须说明并控制其隐私影响。

配置合理的安全响应头，包括 CSP、HSTS、`X-Content-Type-Options`、`Referrer-Policy` 和 `Permissions-Policy`，并按实际部署平台验证配置格式。

## 12. 工程成熟化

保留合理的 React、TypeScript 和 Vite 技术栈，不为制造“成熟感”而盲目引入后端。

### 12.1 版本与基线

- 如果没有 Git，在工作区根目录建立本地 Git 仓库。
- 检查忽略规则和敏感文件后，创建升级前基线。
- 保持改动可追溯、可比较、可回退。

### 12.2 架构

- 拆分超大的 Home 页面。
- 拆分计算引擎、数据来源、维度注册表、状态管理、结果解释和分享模块。
- 建立统一的运行时输入 Schema。
- UI 取值域和模型取值域必须来自同一份 Schema 或常量。
- 避免用散落的中文字符串作为核心协议。
- 计算引擎保持纯净、确定、无副作用、可单独测试。
- 按单一职责和真实可测试边界拆分，而不是为了减少行数机械切文件。

### 12.3 测试

- 引入适合当前项目的单元测试框架。
- 将现有算法验证迁移为正式自动化测试。
- 增加性质测试、边界测试、黄金场景测试和回归快照。
- 增加组件测试。
- 增加关键用户流程 E2E。
- 增加无障碍自动检查。
- 增加响应式和必要的视觉回归检查。
- 所有测试进入统一命令。

### 12.4 质量门禁

- 建立统一的 `npm run check`。
- 至少包含 typecheck、lint、unit、model validation、E2E 和 build；可以根据运行成本拆成快速检查和完整检查，但最终必须执行完整检查。
- 清除所有 lint 错误。
- 不允许通过大面积关闭规则掩盖真实问题。
- 合理例外必须写清楚原因并尽量缩小作用域。

### 12.5 依赖

- 清理没有被业务使用的大量 UI 组件和直接依赖。
- 修复 high/critical 供应链漏洞。
- 锁定受支持的 Node LTS 和包管理器版本。
- 保持 lockfile 一致。
- 增加许可证和依赖审查说明。

### 12.6 生产环境

- 开发检查插件只能在开发模式启用。
- 生产包不得包含 `code-path`、本地源码路径或调试标记。
- 增加 Error Boundary。
- 增加可恢复错误界面。
- 关键流程不得出现 console error。
- 不得把敏感输入写入错误日志。

### 12.7 性能

- 对核心计算进行性能测试。
- 避免每次微小交互执行不必要的重计算。
- 合理拆包或懒加载高级维度。
- 控制初始资源体积。
- 检查动画和大量 DOM 小人对低端手机的影响。

### 12.8 文档

- 将默认 Vite README 改为真实项目文档。
- 增加产品说明。
- 增加模型方法文档。
- 增加数据来源和更新流程。
- 增加隐私说明。
- 增加安全说明。
- 增加本地开发、测试、构建和部署说明。
- 增加 CHANGELOG。
- 增加数据版本和模型版本变更说明。
- 增加升级前后模型场景对比报告。

## 13. 必须复核并实际修复的现有线索

现有只读核查曾发现以下线索。必须重新验证，确认存在后实际修复，不能只写在报告中：

- 生产构建可以通过，但 lint 当前约有 16 个错误。
- 年龄 50 岁可能没有基础人口数据。
- 现有算法脚本没有进入标准测试和持续质量门禁。
- Home 页面约 914 行。
- 计算引擎约 723 行。
- README 仍是 Vite 默认模板。
- 包名和版本仍接近脚手架状态。
- 项目没有成熟的 Git、CI 和发布回滚流程。
- 数据来源只有名称和年份，没有完整可点击原文与参数映射。
- 约 19 项来源混合了直接数据、推算和娱乐假设。
- 生产环境可能无条件启用了开发检查插件。
- 生产 DOM 或构建产物可能泄露源码路径。
- 依赖中可能存在 high 级漏洞。
- 大量 shadcn/UI 文件和依赖可能没有真实使用。
- Google Fonts 存在隐私和国内可用性问题。
- 分享失败没有用户反馈。
- 没有全局 Error Boundary。
- 自定义输入和搜索缺少完整错误或空状态。
- “官方数据”“误差区间”“互相心动概率”等措辞可能过度承诺。
- 页面存在大量过小辅助文字和对比度不足。
- Chip 没有完整的 `aria-pressed` 语义。
- 滑杆缺少明确 label。
- 动态结果缺少 `aria-live`。
- 移动端底栏缺少完整的 `aria-expanded` 和 `aria-controls`。
- 装饰和小人动画可能污染无障碍树。
- 部分触控目标可能小于 44×44。

此外，验证以下更具体的工程风险：

- `Math.random` 是否在 React render 阶段被直接调用。
- Effect 中是否同步 setState 造成级联渲染。
- Fast Refresh 是否因组件文件混合导出而报错。
- 分享卡 `getContext('2d')`、图片编码和下载失败是否缺少防护。
- 生产网络请求是否符合本地计算与隐私声明。
- `.env.local` 等文件是否被正确忽略，并建立密钥扫描与轮换说明。

## 14. 产品、模型与界面的详细验收标准

持续工作并反复修复，直到以下标准尽可能全部满足。

### 14.1 模型与数据

- 18–50 岁每个单岁基础池均有效。
- 年龄区间合并基本守恒。
- 所有输入通过运行时 Schema 校验。
- 非法枚举不能进入引擎。
- 无 NaN、Infinity、负人数和越界概率。
- 所有概率位于 `[0,1]`。
- 加严真正的硬条件不会使结果人数增加。
- OR、AND、互斥、包含和嵌套语义有自动测试。
- 相关条件不会被简单重复相乘。
- 常见组合经过现实锚点校准。
- 极端组合不会被错误显示为现实中绝对不存在。
- 现有算法验证完整保留并扩充。
- 典型场景的模型变更有升级前后差异报告。
- 每个硬筛选因子有来源、口径、公式和可信等级。
- 页面显示 `modelVersion` 和 `dataVersion`。
- 对结果精度进行合理舍入，不制造假精确。

### 14.2 产品与交互

- 分步单页流程完整可用。
- 维度库显著扩充并支持搜索和分类。
- 硬条件、相关硬条件、软偏好和娱乐条件清楚区分。
- 已选条件可以查看、删除、撤销和清空。
- 桌面端结果常驻。
- 手机端结果随时可达。
- 初始、空、非法、极小值、加载、成功、失败和恢复状态齐全。
- 支持结果解释和条件放宽建议。
- 分享前可以预览并控制敏感字段。
- 所有核心流程在返回、修改和异常情况下有合理表现。
- 不存在点击后无反馈的死操作。

### 14.3 视觉、响应式与无障碍

- 在 320、360、390、768、1024、1440px 宽度下均无非预期横向滚动、遮挡或溢出。
- 手机固定底栏考虑 `safe-area-inset-bottom`。
- 200% 页面缩放和系统大字体下仍可使用。
- 完整键盘流程可用。
- 焦点状态清晰可见。
- 选择状态具有正确的 ARIA 语义。
- 标题层级正确。
- 动态结果可以被辅助技术感知。
- 装饰内容从无障碍树隐藏，并提供等价文字摘要。
- 触控目标尽量不小于 44×44。
- 文本、焦点和控件达到 WCAG 2.2 AA。
- axe serious/critical 为 0。
- `prefers-reduced-motion` 模式完整可用。
- 保留品牌特色，同时显著减少视觉噪声。

### 14.4 工程与安全

- 在干净依赖环境中能够安装和运行。
- typecheck 通过。
- lint 为 0 error。
- 单元测试通过。
- 模型验证通过。
- E2E 通过。
- production build 通过。
- 统一 `npm run check` 通过。
- 生产依赖 high/critical 漏洞为 0；如果工具或镜像本身无法提供审计，使用可靠替代方式并记录限制。
- 生产产物不包含源码路径、`code-path` 或开发调试标记。
- 关键流程无 console error。
- 没有提交令牌、密钥和敏感配置。
- README、模型、数据、隐私、安全和变更文档完整。

### 14.5 性能参考目标

- 核心计算在约定测试设备上 p95 尽量低于 50ms。
- INP 尽量低于 200ms。
- CLS 低于 0.1。
- LCP 尽量低于 2.5s。
- Lighthouse Accessibility 尽量达到 95 以上。

如果某项指标受本地环境限制而无法可靠测量，记录测量方法、限制和现有证据，不得虚假宣称通过。

## 15. 必测用户流程与状态矩阵

自动化测试和人工浏览器验证至少覆盖：

1. 首次进入并理解产品边界。
2. 修改基础范围。
3. 添加一个常见硬条件并看到合理变化。
4. 逐步添加多个相关硬条件，确认没有无脑连乘。
5. 添加多个软偏好，确认不会错误砍掉人口池。
6. 添加娱乐条件，确认不污染可信人口估算。
7. 搜索维度、无搜索结果和清除搜索。
8. 查看已选条件、删除单项、撤销、重做和一键清空。
9. 使用预设并处理覆盖确认。
10. 查看普通结果、极小结果和低于模型分辨率的结果。
11. 查看条件影响排行和一键放宽建议。
12. 完成反向自评或双向条件交集流程。
13. 打开分享预览、隐藏敏感字段、生成分享图。
14. 模拟分享生成失败并成功恢复。
15. 键盘完成核心流程。
16. reduced-motion 模式。
17. 无网络或字体加载失败时的降级表现。
18. 手机与桌面关键流程。

状态矩阵至少包含：

- initial/default。
- active/selected。
- hover。
- focus-visible。
- disabled。
- loading。
- success。
- error。
- empty search。
- invalid number。
- no applicable data。
- very small estimate。
- model resolution exceeded。
- share preview。
- share success/failure。
- offline/font fallback。
- reduced motion。

## 16. 交付物要求

不要留下大量 TODO、占位页面、伪实现或只写文档不落地的方案。

在项目中留下结构清晰、可维护的交付物，至少包括：

- 完成后的可运行产品代码。
- 维度注册表和运行时 Schema。
- 数据证据登记表。
- 模型方法文档。
- 数据来源与更新流程。
- 模型版本和数据版本。
- 升级前后典型场景对比报告。
- 单元、性质、边界、黄金场景和 E2E 测试。
- 真实的 README。
- PRIVACY 或等价隐私说明。
- SECURITY 或等价安全说明。
- CHANGELOG。
- 本地运行、测试、构建和部署说明。

## 17. 最终回复要求

完成全部可执行工作后，只提交一次完整总结。最终总结必须包含：

1. 产品最终定位和信息架构。
2. 原来“稍选几个条件就没人”的真实原因及证据。
3. 修复了哪些模型错误。
4. 新模型如何避免无脑连乘。
5. 新增了哪些维度类别和代表性维度。
6. 硬条件、相关硬条件、软偏好和娱乐条件如何区分。
7. 交互、视觉和文案做了哪些升级。
8. 隐私、分享和敏感内容如何处理。
9. 工程架构和测试体系如何升级。
10. 修改和新增的主要文件。
11. 实际运行过的验证命令及结果。
12. 升级前后的代表性场景结果。
13. 桌面端和手机端的视觉验证证据。
14. 未能完全解决的问题、模型局限和原因。
15. 如何在本地运行、测试和构建最终产品。

最终回复不得只说“已完成”或笼统声称通过。所有重要结论都要给出可核查的文件、命令输出、测试结果、场景对比或视觉证据。

现在开始执行。不要先回一份空泛计划，不要阶段性等待用户批准；在授权范围内持续推进，直到形成真正成熟、经过验证的最终产品。
